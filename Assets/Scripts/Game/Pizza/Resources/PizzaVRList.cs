using UnityEngine;
//using UnityEngine.XR.Interaction.Toolkit;

public class PizzaVRList : MonoBehaviour
{
    //[SerializeField] private InteractionLayerMask maskNode;
    //[SerializeField] private InteractionLayerMask maskMixed;

    //[SerializeField] private InteractionLayerMask maskDefault;
    //[SerializeField] private InteractionLayerMask maskInverted;
    //[SerializeField] private InteractionLayerMask maskNothing;

    //[SerializeField] private InteractionLayerMask maskUI;

    //public InteractionLayerMask MaskUI => maskUI;

    //InteractionLayerMask GetPieceType(bool isInverted)
    //{
    //    return (isInverted) ? maskInverted : maskDefault;
    //}
    //InteractionLayerMask GetRayType(bool setWall)
    //{
    //    return (setWall) ? maskMixed : maskNode;
    //}

    //public InteractionLayerMask GetRayMask(bool setWall, bool enabled)
    //{
    //    return (enabled) ? GetRayType(setWall) : maskNothing;
    //}
    //public InteractionLayerMask GetPieceMask(bool isInverted, bool enabled)
    //{
    //    return (enabled) ? GetPieceType(isInverted) : maskNothing;
    //}
}